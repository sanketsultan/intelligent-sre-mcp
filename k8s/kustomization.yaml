# Root entry point — deploys base configuration (no local postgres).
# For local dev with postgres:  kubectl apply -k k8s/overlays/dev
# For production:               kubectl apply -k k8s/overlays/prod
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - base/
