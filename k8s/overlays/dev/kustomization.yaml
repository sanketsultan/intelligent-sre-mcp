# Dev overlay — extends base with local PostgreSQL and demo workloads
# Usage: kubectl apply -k k8s/overlays/dev
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: intelligent-sre

resources:
  - ../../base
  - postgres.yaml
  - demo-metrics.yaml

# Image tag is injected by CI:
#   kustomize edit set image ghcr.io/sanketsultan/intelligent-sre-agent=ghcr.io/sanketsultan/intelligent-sre-agent:<tag>
images:
  - name: ghcr.io/sanketsultan/intelligent-sre-agent
    newTag: latest
